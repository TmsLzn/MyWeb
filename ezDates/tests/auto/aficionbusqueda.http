
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing aficionbusqueda
### This query is used in several of the below tests it should be executed first.
# @name aficionbusqueda
GET {{BASE}}/aficionbusqueda

### Login a(n) usuarios and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing aficionbusqueda by its id.
### positive test 
@afiId = {{aficionbusqueda.response.body.0.aficionBusquedaId}}
GET {{BASE}}/aficionbusqueda/{{afiId}} 
Content-Type: application/json 


### Test 02: Try get one existing aficionbusqueda by its nonexistent id.
### negative test 
GET {{BASE}}/aficionbusqueda/999999999
Content-Type: application/json 


### Test 03: Add a new aficionbusqueda successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newaficionbusqueda
POST {{BASE}}/aficionbusqueda 
Content-Type: application/json 
Token: {{token}} 

{
	"aficion": ###REPLACE###,
	"busquedaId": ###REPLACE###
} 

### Check the created aficionbusqueda

@newaficionbusquedaid = {{newaficionbusqueda.response.body.lastId}}
GET {{BASE}}/aficionbusqueda/{{newaficionbusquedaid}}
Content-Type: application/json 

### Test 04: Add a new aficionbusqueda without a session token
### Negative test 
POST {{BASE}}/aficionbusqueda 
Content-Type: application/json 

{ 
	"aficion": ###REPLACE###,
	"busquedaId": ###REPLACE###
}

### Test 05: Modify an existing aficionbusqueda 
### Positive test 
@afiId = {{aficionbusqueda.response.body.0.aficionBusquedaId}} 
PUT {{BASE}}/aficionbusqueda/{{afiId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"aficion": ###REPLACE###,
	"busquedaId": ###REPLACE###
}

### Check the modified aficionbusqueda

GET {{BASE}}/aficionbusqueda/{{newaficionbusquedaid}}
Content-Type: application/json 

### Test 06: Try to modify an existing aficionbusqueda without a session token
### Negative test
@afiId = {{aficionbusqueda.response.body.0.aficionBusquedaId}}
PUT {{BASE}}/aficionbusqueda/{{afiId}}
Content-Type: application/json

{
	"aficion": ###REPLACE###,
	"busquedaId": ###REPLACE###
}

### Test 07: Delete an existing aficionbusqueda
### Positive test

### Create a new aficionbusqueda, which we will delete
# @name createdafiToDelete
POST {{BASE}}/aficionbusqueda
Content-Type: application/json
Token: {{token}}

{
	"aficion": ###REPLACE###,
	"busquedaId": ###REPLACE###
}

### Check the created aficionbusqueda
@afidelId = {{createdafiToDelete.response.body.lastId}}
GET {{BASE}}/aficionbusqueda/{{afidelId}}
Content-Type: application/json 

### Delete the aficionbusqueda
DELETE {{BASE}}/aficionbusqueda/{{afidelId}}
Token: {{token}}

### Check the deleted aficionbusqueda
GET {{BASE}}/aficionbusqueda/{{afidelId}}
Content-Type: application/json 

### Test 08: Try to delete a aficionbusqueda without a session token
### Negative test
DELETE {{BASE}}/aficionbusqueda/{{afiId}}

