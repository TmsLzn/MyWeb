
### This is an auto-generated test suite, it needs to be completed with valid data.
### These are not all tests you need, more of them should be created to evaluate the functional
### requirements of your project. These tests only test the CRUD endpoints of the entity.
### Silence is a DEAL research team project, more info about us in https://deal.us.es
@BASE = http://127.0.0.1:8080/api/v1

### Auxiliary query
### Positive test
### Test 00: Get all existing ubicaciones
### This query is used in several of the below tests it should be executed first.
# @name ubicaciones
GET {{BASE}}/ubicaciones

### Login a(n) usuarios and save the generated token 
### This token is used in several of the below tests it should be executed second.
# @name login
POST {{BASE}}/login 
Content-Type: application/json 

{ 
	"email": ###REPLACE###,
	"password": ###REPLACE###
}

###
@token = {{login.response.body.sessionToken}}


### TESTS BEGIN

### Test 01: Get one existing ubicaciones by its id.
### positive test 
@ubiId = {{ubicaciones.response.body.0.ubicacionId}}
GET {{BASE}}/ubicaciones/{{ubiId}} 
Content-Type: application/json 


### Test 02: Try get one existing ubicaciones by its nonexistent id.
### negative test 
GET {{BASE}}/ubicaciones/999999999
Content-Type: application/json 


### Test 03: Add a new ubicaciones successfully
### Positive test 
### We assume that the token has been aquired by the login request.
# @name newubicaciones
POST {{BASE}}/ubicaciones 
Content-Type: application/json 
Token: {{token}} 

{
	"codigoPostalId": ###REPLACE###,
	"provinciaId": ###REPLACE###,
	"municipioId": ###REPLACE###
} 

### Check the created ubicaciones

@newubicacionesid = {{newubicaciones.response.body.lastId}}
GET {{BASE}}/ubicaciones/{{newubicacionesid}}
Content-Type: application/json 

### Test 04: Add a new ubicaciones without a session token
### Negative test 
POST {{BASE}}/ubicaciones 
Content-Type: application/json 

{ 
	"codigoPostalId": ###REPLACE###,
	"provinciaId": ###REPLACE###,
	"municipioId": ###REPLACE###
}

### Test 05: Modify an existing ubicaciones 
### Positive test 
@ubiId = {{ubicaciones.response.body.0.ubicacionId}} 
PUT {{BASE}}/ubicaciones/{{ubiId}} 
Content-Type: application/json 
Token: {{token}} 

{ 
	"codigoPostalId": ###REPLACE###,
	"provinciaId": ###REPLACE###,
	"municipioId": ###REPLACE###
}

### Check the modified ubicaciones

GET {{BASE}}/ubicaciones/{{newubicacionesid}}
Content-Type: application/json 

### Test 06: Try to modify an existing ubicaciones without a session token
### Negative test
@ubiId = {{ubicaciones.response.body.0.ubicacionId}}
PUT {{BASE}}/ubicaciones/{{ubiId}}
Content-Type: application/json

{
	"codigoPostalId": ###REPLACE###,
	"provinciaId": ###REPLACE###,
	"municipioId": ###REPLACE###
}

### Test 07: Delete an existing ubicaciones
### Positive test

### Create a new ubicaciones, which we will delete
# @name createdubiToDelete
POST {{BASE}}/ubicaciones
Content-Type: application/json
Token: {{token}}

{
	"codigoPostalId": ###REPLACE###,
	"provinciaId": ###REPLACE###,
	"municipioId": ###REPLACE###
}

### Check the created ubicaciones
@ubidelId = {{createdubiToDelete.response.body.lastId}}
GET {{BASE}}/ubicaciones/{{ubidelId}}
Content-Type: application/json 

### Delete the ubicaciones
DELETE {{BASE}}/ubicaciones/{{ubidelId}}
Token: {{token}}

### Check the deleted ubicaciones
GET {{BASE}}/ubicaciones/{{ubidelId}}
Content-Type: application/json 

### Test 08: Try to delete a ubicaciones without a session token
### Negative test
DELETE {{BASE}}/ubicaciones/{{ubiId}}

